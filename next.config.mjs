/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn-lmhbb.nitrocdn.com', 'images.unsplash.com', 'pbs.twimg.com'],
      },
};

export default nextConfig;
